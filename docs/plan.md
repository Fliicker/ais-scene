本项目需要基于ais数据库构建一个简单的船舶轨迹可视化程序，要求如下：

## 项目架构
vue + node + sqlite，sqlite文件位置在.env文件中指定 

## 业务需求
- 纯mapbox地图界面，每个船舶对应一条轨迹线，用不同颜色展示，并附加船舶名称的注记，
- 轨迹线上显示轨迹点，鼠标移上轨迹点时显示popup，展示时间。
- 左上角有时间选框，选择当前展示轨迹数据的时间范围，可选择现有表中的任意一天，或全部时间。

数据库的数据内容参考 @docs/数据库结构.md


